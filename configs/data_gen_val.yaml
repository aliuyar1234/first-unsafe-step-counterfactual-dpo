eval:
  methods:
    - base
  split: val
  resume_existing: true
  real_run_max_attempts: 3
  eval_seeds: [0, 1, 2]
  metrics:
    primary:
      - hs_mr
      - safe_kpi_sr
    secondary:
      - avg_severity
      - kpi_sr
      - refusal_rate
      - psg
      - first_unsafe_step
  bootstrap:
    enabled: false
  manual_audit:
    audit_severity_threshold_band: [2, 3]
    audit_threshold_crossings: false
    audit_random_fraction: 0.0
